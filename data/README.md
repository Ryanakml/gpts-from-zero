# Data

Keep raw training text files here.

Guidelines:

- start with tiny files for fast iteration
- use UTF-8 plain text where possible
- do not put private or sensitive content here

Example:

- `data/sample_corpus.txt`
